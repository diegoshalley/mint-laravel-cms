@extends('layouts.public') @section('title','Verification required') @section('content')<section class="section"><div class="container narrow"><p class="eyebrow">Staff security</p><h1>Enter your authentication code</h1><p>Use the six-digit code from your authenticator application, or enter one unused recovery code.</p>@if($errors->any())<div class="error-box">{{ $errors->first() }}</div>@endif<form class="panel form" method="post" action="{{ route('mfa.challenge') }}">@csrf<label>Authentication code<input name="code" inputmode="numeric" autocomplete="one-time-code" pattern="[0-9]{6}" maxlength="6"></label><p class="meta">OR</p><label>Recovery code<input name="recovery_code" autocomplete="one-time-code"></label><button class="button">Verify and continue</button></form></div></section>@endsection
