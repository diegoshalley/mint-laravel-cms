@extends('layouts.public') @section('title','Change temporary password') @section('content')<section class="section"><div class="container narrow"><p class="eyebrow">Account security</p><h1>Replace your temporary password</h1><p>Use at least 12 characters. Do not reuse a password from another system.</p>@if($errors->any())<div class="error-box"><ul>@foreach($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif<form class="panel form" method="post" action="{{ route('password.change') }}">@csrf @method('put')<label>Current password<input type="password" name="current_password" required autocomplete="current-password"></label><label>New password<input type="password" name="password" required autocomplete="new-password" minlength="12"></label><label>Confirm new password<input type="password" name="password_confirmation" required autocomplete="new-password" minlength="12"></label><button class="button">Change password</button></form></div></section>@endsection
