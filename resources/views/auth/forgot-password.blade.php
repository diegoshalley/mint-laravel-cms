<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>Reset password · Interior CMS</title><link rel="stylesheet" href="{{ asset('css/app.css') }}"></head><body class="auth-page"><main class="auth-card"><a class="brand" href="{{ route('home') }}"><span class="crest">GH</span><span><strong>INTERIOR CMS</strong><small>Account recovery</small></span></a><h1>Reset password</h1><p>Enter your official email address. For security, we cannot confirm whether an account exists.</p>@if(session('status'))<div class="flash">{{ session('status') }}</div>@endif<form method="post" action="{{ route('password.email') }}">@csrf<label>Email address<input type="email" name="email" value="{{ old('email') }}" required autofocus autocomplete="email"></label>@error('email')<p class="error">{{ $message }}</p>@enderror<button class="button" type="submit">Send reset instructions</button></form><a href="{{ route('login') }}">← Return to sign in</a></main></body></html>
