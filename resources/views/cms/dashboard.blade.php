@extends('layouts.cms') @section('title','Overview') @section('content')<div class="page-title"><div><p class="eyebrow">Editorial control</p><h1>Publishing overview</h1><p>Review what needs attention before content reaches the public.</p></div>@can('content.create')<a class="button" href="{{ route('cms.content.create') }}">Create content</a>@endcan</div><div class="stats">@foreach(['draft'=>'Drafts','in_review'=>'In review','approved'=>'Approved','scheduled'=>'Scheduled','published'=>'Published'] as $key=>$label)<div><span>{{ $label }}</span><strong>{{ $counts[$key] ?? 0 }}</strong></div>@endforeach</div><section class="panel"><div class="section-heading"><h2>Approval queue</h2><a href="{{ route('cms.content.index') }}">All content →</a></div><div class="table-wrap"><table><thead><tr><th>Title</th><th>Status</th><th>Updated</th></tr></thead><tbody>@forelse($queue as $item)<tr><td><a href="{{ route('cms.content.edit',$item) }}">{{ $item->title }}</a></td><td><span class="badge">{{ str_replace('_',' ',$item->status->value) }}</span></td><td>{{ $item->updated_at->diffForHumans() }}</td></tr>@empty<tr><td colspan="3">Nothing is awaiting review or publication.</td></tr>@endforelse</tbody></table></div></section>@endsection
