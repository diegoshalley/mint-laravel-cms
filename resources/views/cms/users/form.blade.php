@extends('layouts.cms') @section('title',$user->exists ? 'Manage staff account' : 'Create staff account') @section('content')<div class="page-title"><div><p class="eyebrow">Access control</p><h1>{{ $user->exists ? 'Manage staff account' : 'Create staff account' }}</h1></div><a href="{{ route('cms.users.index') }}">← Staff accounts</a></div>@if($errors->any())<div class="error-box"><ul>@foreach($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif<div class="editor-grid"><form class="panel form" method="post" action="{{ $user->exists ? route('cms.users.update',$user) : route('cms.users.store') }}">@csrf @if($user->exists)@method('put')@endif<label>Full name<input name="name" value="{{ old('name',$user->name) }}" required></label><label>Official email<input type="email" name="email" value="{{ old('email',$user->email) }}" required></label><label>Role<select name="role" required>@foreach($roles as $role)<option value="{{ $role->name }}" @selected(old('role',$user->getRoleNames()->first())===$role->name)>{{ $role->name }}</option>@endforeach</select></label>@unless($user->exists)<label>Temporary password<input type="password" name="password" required minlength="12" autocomplete="new-password"></label><label>Confirm temporary password<input type="password" name="password_confirmation" required minlength="12" autocomplete="new-password"></label><p>The staff member must replace this password and enroll MFA before CMS access.</p>@endunless<button class="button">{{ $user->exists ? 'Save account' : 'Create account' }}</button></form>@if($user->exists)<aside class="panel workflow"><h2>Account status</h2><p>{{ $user->is_active ? 'This account can sign in.' : 'This account is blocked from signing in.' }}</p>@if($user->is_active)<form method="post" action="{{ route('cms.users.disable',$user) }}">@csrf<button class="button secondary">Disable account</button></form>@else<form method="post" action="{{ route('cms.users.enable',$user) }}">@csrf<button class="button">Enable account</button></form>@endif</aside>@endif</div>@endsection
