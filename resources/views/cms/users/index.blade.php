@extends('layouts.cms') @section('title','Staff accounts') @section('content')<div class="page-title"><div><p class="eyebrow">Access control</p><h1>Staff accounts</h1><p>Provision named accounts, assign one accountable role and disable access immediately when required.</p></div><a class="button" href="{{ route('cms.users.create') }}">Create staff account</a></div><section class="panel"><div class="table-wrap"><table><thead><tr><th>Staff member</th><th>Role</th><th>MFA</th><th>Status</th><th>Last sign-in</th><th></th></tr></thead><tbody>@foreach($users as $user)<tr><td><strong>{{ $user->name }}</strong><br><span class="meta">{{ $user->email }}</span></td><td>{{ $user->getRoleNames()->first() ?? 'Unassigned' }}</td><td>{{ $user->two_factor_confirmed_at ? 'Active' : 'Required' }}</td><td><span class="badge">{{ $user->is_active ? 'Active' : 'Disabled' }}</span></td><td>{{ $user->last_login_at?->diffForHumans() ?? 'Never' }}</td><td><a href="{{ route('cms.users.edit',$user) }}">Manage</a></td></tr>@endforeach</tbody></table></div>{{ $users->links() }}</section>@endsection
