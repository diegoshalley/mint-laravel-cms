@extends('layouts.cms') @section('title','Navigation') @section('content')
<div class="page-title"><div><p class="eyebrow">Site structure</p><h1>Navigation</h1><p>Control the order and visibility of public menu links.</p></div><a class="button" href="{{ route('cms.navigation.create') }}">Add navigation item</a></div>
@if($errors->any())<div class="error-box"><ul>@foreach($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif
<section class="panel"><div class="table-wrap"><table><thead><tr><th>Position</th><th>Label</th><th>Menu</th><th>Destination</th><th>Visibility</th><th>Action</th></tr></thead><tbody>@forelse($items as $item)<tr><td>{{ $item->position }}</td><td><a href="{{ route('cms.navigation.edit',$item) }}">{{ $item->parent_id ? '↳ ' : '' }}{{ $item->label }}</a></td><td>{{ $item->location }}</td><td><code>{{ $item->destination }}</code></td><td><span class="badge">{{ $item->is_visible ? 'visible' : 'hidden' }}</span></td><td><form method="post" action="{{ route('cms.navigation.destroy',$item) }}">@csrf @method('delete')<button class="text-danger">Delete</button></form></td></tr>@empty<tr><td colspan="6">No navigation items exist.</td></tr>@endforelse</tbody></table></div></section>
@endsection
